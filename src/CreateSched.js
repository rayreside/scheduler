import React from 'react';
import arrayMove from 'array-move';

import DisplaySched from './DisplaySched.js';

const CreateSched = ({  tasks, setTasks,
                        tasklist, setTasklist,
                        techf, setTechf,
                        techp, setTechp
                    }) => {

    return(
        <div style={{ flexGrow: 1 }}>
        </div>
    )
}

export default CreateSched;